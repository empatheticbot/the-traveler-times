name: Trigger Netlify Build
on:
  schedule:
    - cron: "*/15 * * * *"
jobs:
  build:
    name: Request Netlify Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/6009d8f817e93e13c3d9f73d
