{% import "components/items.njk" as items %}

{% macro modifierListLayout(title, modifiers) %}
  <section class="modifiers-group">
    <h4 class="modifier-heading">Champions</h4>
    <ul role="list" class="modifier-list">
      {% for modifier in modifiers %}
        <li class="modifier">
          {{ items.inventoryItem(modifier.name, modifier.description, icon=modifier.icon) }}
        </li>
      {% endfor %}
    </ul>
  </section>
{% endmacro %}