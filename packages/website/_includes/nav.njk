{% if page.fileSlug === 'support' or page.fileSlug === 'about' %}
  <nav class="nav-about">
    <a href="/">Home</a>
    <p class="nav-support">
        ❤️ <a href="https://buy.stripe.com/4gw7vdbqwevvg927st">I'd like to show my support</a>
    </p>
    <p class="nav-support">
        🙋‍♀️ <a href="/support/">I have a suggestion</a>
    </p>
    <p class="nav-support">
        🎓 <a href="/about/">I want to learn about the site</a>
    </p>
  </nav>
{% else %}
  <today-date class="small-label page-header-today">{{ computed.buildDate | readableDate}}</today-date>
  <nav class="nav">
    {% for date, activities in nav | groupby("date") %}
      <section class="nav-section">
        <since-date 
          class="small-label"
          datetime="{{date}}"
          >
          {{date | readableDate}}
        </since-date>
        <ul class="nav-list">
          {% for activity in activities %}
            <li class="nav-item">{{ activity.markup | safe}}</li>
          {% endfor %}
        </ul>
      </section>
    {% endfor %}
    <aside class="nav-aside nav-section">
      <p>
        <a href="#season">{{
    season.currentSeason.displayProperties.name
  }}</a> ends <until-date datetime="{{
    season.currentSeason.endDate
  }}">{{season.currentSeason.endDate | readableDate}}</until-date>.</p>
      <p>
        Site last updated <since-date datetime="{{computed.buildDate}}">{{ computed.buildDate | readableDate}}</since-date>.
      </p>
    </aside>
    <aside class="nav-aside nav-section">
      <p class="nav-support">
        ❤️ <a href="https://buy.stripe.com/4gw7vdbqwevvg927st">I'd like to show my support</a>
      </p>
      <p class="nav-support">
        🙋‍♀️ <a href="/support/">I have a suggestion</a>
      </p>
      <p class="nav-support">
        🎓 <a href="/about/">I want to learn about the site</a>
      </p>
    </aside>
  </nav>
{% endif %}