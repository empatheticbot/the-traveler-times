{% if page.fileSlug === 'support' or page.fileSlug === 'about' %}
  <nav class="nav-about">
    <a href="/">Home</a>
    <p class="nav-support">
        ❤️ <a href="https://buy.thetraveler.news">I'd like to show my support</a>
    </p>
    <p class="nav-support">
        🙋‍♀️ <a href="/support">I have a suggestion</a>
    </p>
    <p class="nav-support">
        🎓 <a href="/about">I want to learn about the site</a>
    </p>
  </nav>
{% else %}
  <today-date class="small-label page-header-today"></today-date>
  <nav class="nav">
    {% for date, activities in nav | groupby("date") %}
      <section class="nav-section">
        <since-date 
          class="small-label"
          datetime="{{date}}"
          >
          {{date}}
        </since-date>
        <ul>
          {% for activity in activities %}
            <li class="nav-item">{{ activity.markup | safe}}</li>
          {% endfor %}
        </ul>
      </section>
    {% endfor %}
    <aside class="nav-aside nav-section">
      <p>
        <a href="#season">{{
    season.currentSeason.displayProperties.name
  }}</a> ends <until-date datetime="{{
    season.currentSeason.endDate
  }}"></until-date>.</p>
      <p>
        Site last updated <since-date datetime="{{computed.buildDate}}"></since-date>.
      </p>
    </aside>
    <aside class="nav-aside nav-section">
      <p class="nav-support">
        ❤️ <a href="https://buy.thetraveler.news">I'd like to show my support</a>
      </p>
      <p class="nav-support">
        🙋‍♀️ <a href="/support">I have a suggestion</a>
      </p>
      <p class="nav-support">
        🎓 <a href="/about">I want to learn about the site</a>
      </p>
    </aside>
  </nav>
{% endif %}